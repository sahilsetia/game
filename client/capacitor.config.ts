import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sahil.businessindia',
  appName: 'Business India',
  webDir: 'dist',
};

export default config;
